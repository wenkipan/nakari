[pytest]
markers =
    unit: unit tests (fast, no external deps)
    integration: integration tests
    slow: slow tests (e.g., mocked LLM)

asyncio_mode = auto
addopts =
    -v
    --tb=short
    --strict-markers
